#GUI window creation
from tkinter import *
root = Tk() # create the window
root.geometry("500x500") # define dimensions of the window
root.title("GUI Creation") # window title
root.mainloop() # initiate everything